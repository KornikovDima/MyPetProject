<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:padding="6dp"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <ImageView
        android:id="@+id/person_photo_IV"
        android:layout_width="90dp"
        android:layout_height="160dp"
        android:src="@drawable/image_search"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        android:contentDescription="@string/person_photo" />

    <TextView
        android:id="@+id/person_name_TV"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:text="@string/name"
        android:textStyle="italic"
        app:layout_constraintStart_toStartOf="@+id/person_photo_IV"
        app:layout_constraintTop_toBottomOf="@+id/person_photo_IV"
        app:layout_constraintEnd_toEndOf="@+id/person_photo_IV"/>


</androidx.constraintlayout.widget.ConstraintLayout>